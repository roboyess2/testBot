'use strict'

var a;
let b;
const d = {};

b = [1, 2, 3]
console.log(b[0]) // 1

let score = 0;

function plusOne(count) {
    count += 1;
}

plusOne(score)

API_URL = ''

const incrementScore = b.map((item) => {
    if (item !== 3) {
        console.log(item)
    }
})

console.log(incrementScore)

